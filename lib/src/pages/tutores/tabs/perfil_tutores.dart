import 'package:flutter/material.dart';

class PerfilTutores extends StatelessWidget {
  const PerfilTutores({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(
        child: Text('PerfilTutores'),
      ),
    );
  }
}
