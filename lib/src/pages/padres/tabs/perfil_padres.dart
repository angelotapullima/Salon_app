import 'package:flutter/material.dart';

class PerfilPadres extends StatelessWidget {
  const PerfilPadres({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(
        child: Text('PerfilPadres'),
      ),
    );
  }
}
