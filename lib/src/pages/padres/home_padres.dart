import 'package:flutter/material.dart';

class HomePadres extends StatelessWidget {
  const HomePadres({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}